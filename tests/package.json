{
  "name": "treasure-hunt-tests",
  "private": true,
  "description": "Playwright suites for the Treasure Hunt app; run from this directory with npx playwright test",
  "scripts": {
    "test": "playwright test"
  },
  "dependencies": {
    "@playwright/test": "1.56.1"
  }
}
